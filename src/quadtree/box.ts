import { Vec2D } from '../math/vec2d';

/**
 * This Interface represents a bounding box
 *
 * @author William Reetz
 * @export
 * @interface Box
 */
export interface Box {
    pos: Vec2D,
    dim: Vec2D
}
